import FlowState from "./FlowState";
import KeystrokeEvent from "./KeystrokeEvent";

export function next_state(currentState: FlowState, events: KeystrokeEvent[]): FlowState {
    //TODO
    return currentState;
}